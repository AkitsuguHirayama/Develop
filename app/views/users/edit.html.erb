      
      <nav class="navbar navbar-expand-lg navbar-light bg-light fixed-top">
      <div class="navbar-brand"><h2 class="title"><%= current_user.name %>さんの情報を登録・編集</h2></div>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
        
        <div class="collapse navbar-collapse justify-content-end" id="navbarSupportedContent">
          <ul class="navbar-nav">
            <li class="nav-item active">
              <a class="nav-link" href="#">Home<span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item">
              <%= link_to '妊娠日又は出産日の登録・編集',"#", class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to 'メールアドレスの変更', "#", class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to 'パスワードの変更', "#", class: "nav-link" %>
            </li>
          </ul>
        </div>
        </nav>
        
        <div class="top-hero-wrapper text-center contents">
         <div class="container top-hero-container">
          <%= form_for @user do |f| %>
            <div class="form-group">
              <%= f.label :birthday %><br>
              <%= f.date_field :birthday, class: 'date-form' %>
            </div>
            
            <div class="form-group">
              <%= f.label :pregnantday %><br>
              <%= f.date_field :pregnantday, class: 'date-form' %>
            </div>
            
          <%= f.submit '送信', class: 'btn btn-black' %>
          
          <% end %>
          </div>
        </div>